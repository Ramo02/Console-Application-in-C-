The application prompts the user to input the coordinates of Point A, Point B, and Point C. 
It then calculates the length of each side using the distance formula. It also checks whether the triangle is equilateral, isosceles, or right using the corresponding formulas. 
Finally, it calculates the perimeter of the triangle and outputs even numbers from 0 to the perimeter. 